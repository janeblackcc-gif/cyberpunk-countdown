# 🚀 快速启动指南

## 📋 前置要求

确保您的系统已安装：
- Node.js 18+ （推荐 LTS 版本）
- npm 或 yarn 或 pnpm

## 🔧 安装步骤

### 1. 安装依赖

在项目根目录下运行：

```bash
npm install
# 或使用 yarn
yarn install
# 或使用 pnpm
pnpm install
```

### 2. 启动开发服务器

```bash
npm run dev
# 或使用 yarn
yarn dev
# 或使用 pnpm
pnpm dev
```

浏览器将自动打开 `http://localhost:3000`，您将看到赛博朋克风格的倒计时组件。

### 3. 构建生产版本

```bash
npm run build
# 或使用 yarn
yarn build
# 或使用 pnpm
pnpm build
```

构建产物将生成在 `dist/` 目录中。

### 4. 预览生产版本

```bash
npm run preview
# 或使用 yarn
yarn preview
# 或使用 pnpm
pnpm preview
```

## 📁 项目结构

```
lftbd/
├── index.html              # HTML 入口文件
├── main.jsx                # React 入口文件
├── App.jsx                 # 应用主组件（使用示例）
├── CyberpunkCountdown.jsx  # 倒计时组件（核心）
├── index.css               # Tailwind CSS 引入
├── package.json            # 项目配置
├── vite.config.js          # Vite 配置
├── tailwind.config.js      # Tailwind 配置
├── postcss.config.js       # PostCSS 配置
├── README.md               # 详细文档
└── QUICKSTART.md           # 本文件
```

## ⚙️ 自定义倒计时目标日期

编辑 [App.jsx](App.jsx)：

```jsx
import CyberpunkCountdown from './CyberpunkCountdown';

function App() {
  // 修改这里的日期
  const targetDate = new Date('2025-12-31T23:59:59');

  return <CyberpunkCountdown targetDate={targetDate} />;
}

export default App;
```

## 🎨 效果说明

组件包含以下交互效果：

1. **实时倒计时**：每秒更新剩余时间（天/时/分/秒）
2. **悬停特效**：鼠标移到数字上时：
   - 数字放大 1.1 倍
   - 出现青色光晕
   - 触发 RGB 色差分离（故障风）
   - 轻微抖动效果
3. **倒计时结束**：显示 "SYSTEM ONLINE" 和 CPU 旋转动画

## 🐛 常见问题

### Q: 页面显示空白？

**A**: 检查浏览器控制台是否有错误。确保：
- 依赖已正确安装（`node_modules` 目录存在）
- 开发服务器正在运行（`npm run dev`）

### Q: 图标不显示？

**A**: 确保 `lucide-react` 已安装：

```bash
npm install lucide-react
```

### Q: Tailwind 样式不生效？

**A**: 检查：
1. `tailwind.config.js` 的 `content` 配置是否包含所有 `.jsx` 文件
2. `index.css` 是否正确引入了 Tailwind 指令
3. 重启开发服务器

### Q: 如何部署到生产环境？

**A**: 运行 `npm run build`，然后将 `dist/` 目录部署到任意静态托管服务：
- Vercel
- Netlify
- GitHub Pages
- Cloudflare Pages

## 📚 更多文档

详细使用说明和 API 文档请参考 [README.md](README.md)。

## 🎯 设计理念

本组件严格遵循赛博朋克美学设计原则：
- **High Contrast**：高对比度配色（霓虹 vs 深黑）
- **Glitch Art**：故障风视觉效果
- **Neon Glow**：多层发光阴影
- **HUD Elements**：科技感装饰元素

---

**享受您的赛博朋克之旅！** 🌆✨
